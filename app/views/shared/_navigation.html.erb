<nav class="navbar navbar-default">
  <div class="container">
    <ul class="nav navbar-nav">
      <li class="navbar-back">
        <% if content_for?(:back_url) %>
          <%= link_to "≪ #{t("back")}", yield(:back_url), class: "btn btn-default navbar-btn" -%>
        <% else %>
          <%= link_to "≪ #{t("back")}", "#", class: "btn disabled btn-default navbar-btn", disabled: true -%>
        <% end %>
      </li>

      <li class="navbar-title">
        <span class="navbar-text"><%= yield(:title) -%></span>
      </li>

      <li class="navbar-sign-out">
        <span class="navbar-text">
          <%= t("signed_in_as") -%> <span class="navbar-email"><%= current_user.email -%></span>
        </span>
        <%= button_to t("sign_out"), destroy_user_session_path, method: :delete, class: "btn btn-default navbar-btn" -%>
      </li>
    </ul>
  </div>
</nav>
